import { Typography } from "@mui/material";
import { Box } from "@mui/system";

const FAQ = () => {
	return (
		<Box>
			<Typography variant="h1" fontWeight={600}>
				FAQ
			</Typography>
		</Box>
	);
};

export default FAQ;
