import { Typography } from "@mui/material";
import { Box } from "@mui/system";

const Team = () => {
	return (
		<Box>
			<Typography variant="h1" fontWeight={600}>
				Team
			</Typography>
		</Box>
	);
};

export default Team;
