import { Typography } from "@mui/material";
import { Box } from "@mui/system";

const Pie = () => {
	return (
		<Box>
			<Typography variant="h1" fontWeight={600}>
				Pie
			</Typography>
		</Box>
	);
};

export default Pie;
