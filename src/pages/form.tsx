import { Typography } from "@mui/material";
import { Box } from "@mui/system";

const Form = () => {
	return (
		<Box>
			<Typography variant="h1" fontWeight={600}>
				Form
			</Typography>
		</Box>
	);
};

export default Form;
