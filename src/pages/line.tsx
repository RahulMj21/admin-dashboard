import { Typography } from "@mui/material";
import { Box } from "@mui/system";

const Line = () => {
	return (
		<Box>
			<Typography variant="h1" fontWeight={600}>
				Line
			</Typography>
		</Box>
	);
};

export default Line;
