import { Typography } from "@mui/material";
import { Box } from "@mui/system";

const Bar = () => {
	return (
		<Box>
			<Typography variant="h1" fontWeight={600}>
				Bar
			</Typography>
		</Box>
	);
};

export default Bar;
